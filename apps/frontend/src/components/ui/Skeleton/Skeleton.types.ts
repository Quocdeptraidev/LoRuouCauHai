import type { CSSProperties } from 'react'

export interface SkeletonProps {
  /** Tailwind CSS classes để tuỳ chỉnh kích thước và hình dạng */
  className?: string
  /** Custom inline styles để gán trực tiếp cho thẻ div */
  style?: CSSProperties
}
